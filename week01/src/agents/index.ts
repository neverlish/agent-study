import { describeService } from './service';
import { productPrice } from './price';
import { getDeveloperLinks } from './developer';

export const availableFunctions = {
  getDeveloperLinks,
  productPrice,
  describeService,
};